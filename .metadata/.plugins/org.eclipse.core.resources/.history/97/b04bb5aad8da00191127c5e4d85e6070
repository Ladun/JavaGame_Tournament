package com.ladun.game.Item;

import java.awt.Color;

import com.ladun.engine.GameContainer;
import com.ladun.engine.Renderer;
import com.ladun.game.GameManager;

public class Tooltip {
	
	public class Content{
		public String str;
		public int color;
		
		public Content(String str, int color) {
			this.str =str;
			this.color =color;
		}
	}
	private boolean active;
	private Content[] content;
	private int posX,posY;
	
	public void update(GameContainer gc, GameManager gm) {
		posX = gc.getInput().getMouseX();
		posY = gc.getInput().getMouseY();
	}
	
	public void render(GameContainer gc, Renderer r) {
		if(content == null || content.length == 0)
			return;
		r.drawFillRect(posX, posY,  200, 20 * content.length , 0, 0xffaaaaaa);
		
		for(int i = 0; i < content.length; i++) {
			r.drawText(content[i].str, posX, posY + i * 18, content[i].color);
		}
	}

	public void setting(Content[] content,int posX,int posY) {
		this.content = content;
		this.posX = posX;
		this.posY = posY;
	}

	public boolean isActive() {
		return active;
	}

	public void setActive(boolean active) {
		this.active = active;
	}
}
